# Durable Delegation Security Audit

Status: test-backed checklist for the durable `delegate_task` cutover.

`delegate_task` no longer starts synchronous child sessions. It creates a fixed durable Task graph and returns the Task handle immediately. The Task scheduler and fenced Attempts own execution, cancellation, retry, recovery, usage, results, approvals, and settlement. `SubagentRegistry` is only ephemeral visibility for an Attempt that is currently running.

## Checklist

| Item | Required behavior | Coverage |
|------|-------------------|----------|
| Durable-only availability | `delegate_task` is omitted when profile-bound SQLite Task persistence is unavailable; there is no in-memory execution fallback. | `src/tools/delegation-tools.test.ts`, `src/runtime/create-runtime.test.ts` |
| Provider-call idempotency | Visible-turn identity and stable provider tool-call identity jointly derive the Task creation key. Exact replay within that request scope returns the existing handle; a reused call ID in a later visible turn creates distinct work, while changing explicit or default synthesis semantics or any other graph input within one scope fails closed. | `src/delegation/durable-delegation-service.test.ts`, `src/tools/delegation-tools.test.ts` |
| Immediate return | The tool persists a queued Task and returns its handle without waiting for a worker or result. If post-commit foreground activation fails, it still returns that durable handle with a redacted activation marker. | `src/delegation/durable-delegation-service.test.ts`, `src/tools/delegation-tools.test.ts`, `src/smoke/cases/delegation-mvp.ts` |
| Lifecycle observability | The primary Attempt records bounded provider-completed and result-captured milestones. Status projects those with existing Result, Attempt, Task, and delivery timestamps in order without persisting raw output, arguments, message bodies, or paths. Publication and Task settlement remain atomic, and no second reconciliation path is introduced. Immediate UI copy says `task created` / `تم إنشاء المهمة`; observed child settlements retain truthful outcome summaries. | `src/tasks/agent-step-executor.test.ts`, `src/tasks/task-scheduler.test.ts`, `src/cli/task-commands.test.ts`, `src/ui/papyrus/operator-console/activeWorkRuntimeMapper.test.ts` |
| Batch bounds | A batch becomes one Task with one independent Step per item; persisted Task concurrency is capped by `maxConcurrentChildren` and graph limits. | `src/delegation/durable-delegation-service.test.ts`, `src/smoke/cases/delegation-mvp.ts` |
| Root/child ownership | Root Tasks retain creator-session ownership. Nested calls persist `parentTaskId`, `parentAttemptId`, and an agent actor bound to the active Attempt. New children require the exact current, unexpired parent lease generation and no pending cancellation; stale workers may only recover an exact existing handle. | `src/delegation/durable-delegation-service.test.ts`, `src/tasks/agent-step-executor.test.ts`, `src/tasks/sqlite-task-store.test.ts` |
| Child lifecycle semantics | Every Step persists `forbid` or `fire_and_forget`. Worker Steps forbid runtime children; authorized orchestrators may create only detached children. Parent dependencies are never mutated and parent completion does not wait for detached children. | `src/contracts/task.test.ts`, `src/delegation/durable-delegation-service.test.ts`, `src/tasks/sqlite-task-store.test.ts` |
| Child origin visibility | Descendants inherit root Task and origin-session/turn attribution. Creation atomically links the originating session as an observer, and inspection exposes only bounded child handles and statuses. | `src/delegation/durable-delegation-service.test.ts`, `src/ui/papyrus/operator-console/taskSurface.test.ts` |
| Child authority monotonicity | A child Task is permitted only from an orchestrator Step with remaining child depth; toolsets, exact tools, blocked tools, risk dispositions, and depth can only narrow. | `src/delegation/durable-delegation-service.test.ts`, `src/delegation/toolset-security.test.ts`, `src/contracts/task.test.ts` |
| Child budget monotonicity | A linked child Task writes an atomic immutable reservation. Repeated calls share the active parent Step's call/token/cost ceiling; wall-clock and root live concurrency remain hard tree ceilings; actual usage rolls up through every ancestor. | `src/delegation/durable-delegation-service.test.ts`, `src/tasks/sqlite-task-store.test.ts` |
| Workspace boundary | Root delegation requires live trust. Child Tasks retain the exact parent workspace binding, and execution rechecks trust. | `src/delegation/durable-delegation-service.test.ts`, `src/tasks/agent-step-executor.test.ts` |
| Tool/schema bounds | Parent visibility, default risk classes, exact/prefix blocks, excluded toolsets, and explicit tool/toolset requests narrow the persisted Step authority before provider schemas are built. | `src/delegation/toolset-security.test.ts`, `src/delegation/delegation-mvp-security-audit.test.ts`, `src/smoke/cases/delegation-mvp.ts` |
| Access admission and provenance | Parent visibility comes from the final provider-visible inventory. Missing explicit tools/toolsets and zero effective access fail before graph persistence with structured diagnostics. Successful Steps persist a bounded access audit; exact replay retains it, and runtime widening or zero access fails before a provider call. | `src/delegation/durable-delegation-service.test.ts`, `src/runtime/agent-loop-builder.test.ts`, `src/tasks/agent-step-executor.test.ts`, `src/tools/delegation-tools.test.ts` |
| Research evidence contracts | Research scopes are bounded and distinct. Live-source and repository requirements are admitted only with effective supporting tools, persisted with the Step, and verified against successful tool-execution records before Result acceptance. Unmet contracts produce diagnostic-only `evidence-contract-unsatisfied` output that synthesis cannot consume. | `src/contracts/task.test.ts`, `src/delegation/durable-delegation-service.test.ts`, `src/tasks/agent-step-executor.test.ts`, `src/tools/delegation-tools.test.ts` |
| Requested-answer ownership | A successful non-terminal root delegation with a primary Result Step stops parent provider continuation and replaces provider-authored final text with a deterministic Task acknowledgement. Nested and inspection-only work does not acquire user-facing ownership. Terminal no-answer Tasks deliver a deterministic failure receipt through the existing CLI or external outbox. | `src/runtime/delegated-answer-ownership.test.ts`, `src/runtime/provider-turn-loop.test.ts`, `src/runtime/agent-loop.test.ts`, `src/tasks/task-session-completion.test.ts`, `src/tasks/task-completion-delivery.test.ts` |
| Hardline and approvals | Normal runtime security and hardline checks remain non-overridable. Task approval waits are durable and scoped to Task/Step/Attempt ownership. | `src/tasks/task-approval-service.test.ts`, `src/tasks/agent-step-executor.test.ts` |
| Durable cancellation/recovery | Cancellation signals, leases, fencing tokens, restart reconciliation, retry eligibility, and settlement are owned by the Task scheduler. Its lease guard starts before asynchronous executor work, stops renewal after bounded abort or stale-write grace, and records the lease fence and heartbeat timing when expiry is reconciled. | `src/tasks/task-scheduler.test.ts`, `src/tasks/task-vertical-slice.test.ts` |
| Background runtime lifecycle | New workspace-runtime construction cannot exceed remaining profile scheduler capacity. Failed cleanup is boundedly observable, retains its runtime reference with execution disabled, and remains retryable on later ticks and shutdown. | `src/tasks/supervisor-task-background-host.test.ts`, `src/gateway/supervisor.test.ts` |
| Scoped scheduling queries | Session, host-fence, workspace, origin, and status predicates are applied in the profile-bound store before limits. Keyset pages and oldest-first/rotating admission prevent unrelated rows or newer Tasks from starving authorized older work. | `src/tasks/sqlite-task-store.test.ts`, `src/tasks/task-operator-service.test.ts`, `src/tasks/task-scheduler.test.ts`, `src/session/session-cost-usage.test.ts` |
| Durable result/usage storage | Full result bodies and structured provider usage are persisted against the Task Attempt, with profile and size boundaries. | `src/tasks/task-result-service.test.ts`, `src/tasks/agent-step-executor.test.ts` |
| Worker ownership | Worker sessions carry Task/Step/Attempt metadata, ancestry-derived nesting depth, and durable session links. The same validated depth reaches registry, progress, and diagnostic metadata; in-memory subagent state is not an authorization or ownership boundary. | `src/runtime/agent-loop-factory.test.ts`, `src/tasks/agent-step-executor.test.ts` |
| Model overrides | Same-provider and reviewed cross-provider overrides preserve route configuration, resolve profile credentials, reject disabled routes, and disable child fallback routes. | `src/runtime/agent-loop-factory.test.ts`, `src/tools/delegation-tools.test.ts` |

## Removal assertion

The synchronous `DelegationManager`, `BatchRunner`, and parent-result `file-state-guard` execution architecture has been deleted. The remaining `ChildRunner` is an Attempt execution helper used only after the durable scheduler leases an agent Step; it does not create, schedule, own, or settle Tasks.

This cutover intentionally does not preserve compatibility with the old synchronous result shape. Callers receive a durable Task handle and observe status, results, approvals, cancellation, and completion through Task surfaces.
